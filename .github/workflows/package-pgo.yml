name: Package Citron AppImage
on:
  workflow_dispatch:

jobs:
  package:
    runs-on: ubuntu-latest
    container: archlinux:latest
    steps:
      - name: Install dependencies
        run: |
          pacman -Syu --noconfirm
          # Added xorg-server-xvfb, xorg-xauth, and strace
          pacman -S --noconfirm wget git tar zstd qt6-base qt6-translations sed awk \
                                xorg-server-xvfb xorg-xauth strace

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Packaging Script
        env:
          APP_VERSION: ${{ github.run_number }}
          DEVEL: 'true'
        run: |
          chmod +x package.sh
          # sharun will automatically detect xvfb-run once installed
          ./package.sh

      - name: Upload Finished AppImage
        uses: actions/upload-artifact@v4
        with:
          name: Citron-AppImage
          path: dist/*
